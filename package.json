{
  "name": "text-review-engine",
  "private": true,
  "version": "0.0.8",
  "description": "Obsidian text review engine MVP",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "postinstall": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  },
  "dependencies": {
    "mammoth": "^1.12.0"
  }
}
